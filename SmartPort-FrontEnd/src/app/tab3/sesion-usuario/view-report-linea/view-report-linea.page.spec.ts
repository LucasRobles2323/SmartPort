import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ViewReportLineaPage } from './view-report-linea.page';

describe('ViewReportLineaPage', () => {
  let component: ViewReportLineaPage;
  let fixture: ComponentFixture<ViewReportLineaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ViewReportLineaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
